import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.zhepdelivery.com',
  appName: 'Zhep_Delivery_Boy',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
